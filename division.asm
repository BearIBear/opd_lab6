DIVISIBLE: WORD ?
DIVIDE_BY: WORD ?
DIV_RESULT: WORD ?
DIVIDE:
	LD &2
	ST $DIVISIBLE
	LD &1
	ST $DIVIDE_BY
	DIVISION:
		LD $DIVISIBLE
		CMP $DIVIDE_BY
		BMI STOP
		SUB $DIVIDE_BY
		ST $DIVISIBLE
		LD $DIV_RESULT
		INC
		ST $DIV_RESULT
		JUMP $DIVISION
	STOP:
	LD $DIV_RESULT
	ST &2
RET